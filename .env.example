OPENROUTER_API_KEY=

# Comma-separated frontend origins allowed to call this API, e.g.
# CORS_ORIGINS=http://localhost:3000,https://your-frontend.example.com
# Defaults to "*" (any origin) if unset -- fine for local dev, tighten before deploying
# anywhere reachable outside your own machine.
CORS_ORIGINS=*
